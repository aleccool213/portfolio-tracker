<fieldset class="field-group"
          data-account-fields-target="group"
          data-kinds="<%= Products.kinds_for(Products::Asset).join(" ") %>">
  <legend>Asset details</legend>
  <p class="form-hint">
    Nothing extra to fill in — what it's worth comes from the monthly check-in.
  </p>
</fieldset>
